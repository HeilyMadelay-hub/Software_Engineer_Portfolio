@model IEnumerable<Business_School.Models.Event>
@{
    ViewData["Title"] = "Corregir Eventos con Fechas Inválidas";
    var totalInvalid = (int)(ViewData["TotalInvalid"] ?? 0);
}

<div class="container mt-4">
    <div class="row mb-4">
  <div class="col-md-8">
          <h2>
          <i class="bi bi-calendar-x text-warning"></i> Eventos con Fechas Inválidas
            </h2>
    <p class="text-muted">
        Estos eventos tienen fechas inválidas (01/01/0001) que deben corregirse para aparecer correctamente en el sistema.
            </p>
        </div>
        <div class="col-md-4 text-end">
  <a asp-action="Index" asp-controller="Dashboard" class="btn btn-outline-secondary">
 <i class="bi bi-arrow-left"></i> Volver al Dashboard
    </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
     <div class="alert alert-success alert-dismissible fade show" role="alert">
   <i class="bi bi-check-circle"></i> @TempData["Success"]
     <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
       <i class="bi bi-exclamation-triangle"></i> @TempData["Error"]
     <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Warning"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
    <i class="bi bi-exclamation-circle"></i> @TempData["Warning"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (totalInvalid == 0)
    {
     <div class="alert alert-success">
    <i class="bi bi-check-circle-fill"></i>
   <strong>¡Perfecto!</strong> No hay eventos con fechas inválidas. Todos los eventos están correctos.
        </div>
    }
    else
    {
        <div class="card shadow-sm mb-4">
 <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
   <span>
         <i class="bi bi-exclamation-triangle"></i>
            <strong>@totalInvalid evento(s)</strong> con fechas inválidas
   </span>
 <div>
        <form asp-action="FixAllInvalidEvents" method="post" class="d-inline" 
        onsubmit="return confirm('¿Corregir todos los eventos? Se asignarán fechas a partir de 30 días desde hoy.');">
        @Html.AntiForgeryToken()
         <button type="submit" class="btn btn-success btn-sm me-2">
            <i class="bi bi-check-all"></i> Corregir Todos (30 días desde hoy)
  </button>
          </form>
<form asp-action="DeleteAllInvalidEvents" method="post" class="d-inline"
         onsubmit="return confirm('?? ¿ELIMINAR todos los eventos con fechas inválidas? Esta acción NO se puede deshacer.');">
           @Html.AntiForgeryToken()
   <button type="submit" class="btn btn-danger btn-sm">
  <i class="bi bi-trash"></i> Eliminar Todos
   </button>
 </form>
        </div>
            </div>
            <div class="card-body p-0">
   <div class="table-responsive">
                    <table class="table table-hover table-striped mb-0">
    <thead class="table-dark">
  <tr>
 <th>ID</th>
                 <th>Título</th>
       <th>Departamento</th>
      <th>Fecha Actual</th>
   <th>Nueva Fecha</th>
                <th>Acciones</th>
        </tr>
      </thead>
          <tbody>
           @foreach (var ev in Model)
            {
   var defaultNewDate = DateTime.Now.AddDays(30).ToString("yyyy-MM-ddTHH:mm");
         <tr>
     <td>@ev.Id</td>
                  <td><strong>@ev.Title</strong></td>
    <td>@(ev.Department?.Name ?? "Sin departamento")</td>
   <td>
     <span class="badge bg-danger">
     @ev.StartDate.ToString("dd/MM/yyyy HH:mm")
      </span>
   </td>
    <td>
       <form asp-action="FixEventDate" method="post" class="d-flex gap-2" id="form-@ev.Id">
     @Html.AntiForgeryToken()
             <input type="hidden" name="eventId" value="@ev.Id" />
       <input type="datetime-local" name="newStartDate" class="form-control form-control-sm" 
       value="@defaultNewDate" required style="width: 200px;" />
    <button type="submit" class="btn btn-primary btn-sm">
               <i class="bi bi-check"></i> Guardar
    </button>
   </form>
       </td>
      <td>
         <a asp-action="Edit" asp-controller="Events" asp-route-id="@ev.Id" 
    asp-route-returnUrl="@Url.Action("FixInvalidEvents", "Admin")" 
  class="btn btn-outline-primary btn-sm">
    <i class="bi bi-pencil"></i> Editar Completo
      </a>
       </td>
      </tr>
     }
          </tbody>
         </table>
 </div>
   </div>
        </div>
  }

    <div class="card shadow-sm">
     <div class="card-header bg-info text-white">
        <i class="bi bi-info-circle"></i> ¿Por qué ocurre esto?
      </div>
        <div class="card-body">
            <p>Los eventos con fecha <code>01/01/0001 00:00</code> indican que la fecha no se guardó correctamente al crear o editar el evento.</p>
        <p>Esto puede ocurrir por:</p>
            <ul>
     <li>El campo de fecha no se rellenó correctamente en el formulario</li>
       <li>Problemas de formato en el input <code>datetime-local</code></li>
          <li>Errores de binding en el modelo</li>
</ul>
       <p class="mb-0"><strong>Este problema ya ha sido corregido</strong> en los formularios de creación y edición de eventos.</p>
        </div>
    </div>
</div>
